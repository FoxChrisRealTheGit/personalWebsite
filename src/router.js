import React from 'react';
import { Switch, Route } from 'react-router';

export default (
    <Switch>
        <Route to="" component></Route>
        <Route to="" component></Route>
        <Route to="" component></Route>
    </Switch>

)