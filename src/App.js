import React, { Component } from 'react';
import Router from '../src/router.js';

class App extends Component {
  render() {
    return (
      <section>
        {Router}
      </section>
    );
  }
}

export default App;
